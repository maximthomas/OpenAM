# Vite mode file for `npm run dev:mock` (vite --mode mock). Turns on the in-browser MSW worker
# so the EUI app runs against the shared AM-REST handlers with no OpenAM backend (ADR-0010, P0-8).
# Only loaded under `--mode mock`; never set for `npm run dev` or the production build.
VITE_MOCK=true
